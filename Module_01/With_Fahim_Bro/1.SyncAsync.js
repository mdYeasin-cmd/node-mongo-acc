const colors = require("colors");

//? START

console.log("Welcome to ACC.");

setTimeout(() => {
    const user = { id: 1, name: "Mr. Fahim" };
    setTimeout(() => {
        const products = [{}, {}, {}];
        setTimeout(() => {
            const prices = [];
        }, 1000);
    }, 500);
}, 1000);

console.log(user);

console.log("Hope you are also excited");
